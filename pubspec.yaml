name: zezo_kids

description: A parental control app for ZezoKids.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter  
  camera: ^0.9.4
  microphone: ^3.0.0
  location: ^4.3.0
  firebase_core: ^2.8.0
  firebase_auth: ^4.4.0
  cloud_firestore: ^4.4.0
  provider: ^6.1.4
  flutter_bloc: ^8.0.0
  cupertino_icons: ^1.0.5
  flutter_svg: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true